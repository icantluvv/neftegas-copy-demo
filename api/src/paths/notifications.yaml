get:
  summary: Список уведомлений текущего пользователя
  operationId: getNotifications
  tags: [Notifications]
  responses:
    '200':
      description: Уведомления текущего пользователя
      content:
        application/json:
          schema:
            type: array
            items: { $ref: '../components/schemas/notification.yaml' }
