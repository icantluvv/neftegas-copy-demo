post:
  summary: Вход по email и паролю
  operationId: login
  tags: [Auth]
  security: []
  requestBody:
    required: true
    content:
      application/json:
        schema: { $ref: '../components/schemas/auth.yaml#/LoginRequest' }
  responses:
    '200':
      description: Успешный вход, устанавливает httpOnly cookie с токенами
      content:
        application/json:
          schema: { $ref: '../components/schemas/auth.yaml#/AuthUser' }
    '400':
      description: Некорректный формат email
      content:
        application/json:
          schema: { $ref: '../components/schemas/error-response.yaml' }
    '401':
      description: Неверный email/пароль или заблокированная учётная запись
      content:
        application/json:
          schema: { $ref: '../components/schemas/error-response.yaml' }
