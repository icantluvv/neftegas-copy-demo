type: object
required: [id, correctionId, timestamp, text]
properties:
  id: { type: integer }
  correctionId: { type: integer }
  timestamp: { type: string, format: date-time }
  userId: { type: [integer, 'null'] }
  user:
    oneOf:
      - $ref: './user.yaml#/UserSummary'
      - type: 'null'
  text: { type: string }
