## Context

`apps/frontend/src/components/notification-bell/notification-bell.tsx` уже
опрашивает `useGetNotifications({ query: { refetchInterval: 60_000 } })` и
уже использует `useMarkAllNotificationsRead` на странице `/notifications`
(`apps/frontend/app/(private)/notifications/components/notifications-content.tsx`,
из `notifications-center`) — тот же мутационный хук переиспользован здесь для
кнопки внутри панели.

## Незаархивированный конфликт с `notifications-center`

`openspec/changes/notifications-center/specs/notifications/spec.md`
(требование «Содержимое выпадающей панели уведомлений») содержит:

> Панель SHALL NOT содержать кнопку «Отметить все прочитанными» — массовая
> пометка доступна только со страницы `/notifications`

и отдельный сценарий «В панели нет кнопки массовой пометки». Задачи 3.10–3.11
`tasks.md` того change явно фиксируют это как пересмотренное продуктовое
решение (кнопку сознательно убрали из панели после первой реализации).

В этой сессии пользователь запросил кнопку обратно в панель — реализовано.
Так как `notifications-center` не заархивирован, `openspec/specs/notifications/`
как капабилити ещё не существует в базовой спеке репозитория: формально
нечего помечать `MODIFIED`. Правильная последовательность действий —
**до архивации любого из двух changes** свести это расхождение: либо
обновить `notifications-center/specs/notifications/spec.md` напрямую (убрать
`SHALL NOT` и связанный сценарий, раз решение снова пересмотрено), либо
архивировать `notifications-center` как есть и заводить полноценный
`MODIFIED Requirements` в `desktop-notifications` постфактум. Решение не
принято в рамках этой сессии — оставлено как блокер в `tasks.md`.

## Goals / Non-Goals

**Goals:**
- Уведомлять пользователя о новых непрочитанных записях, даже если вкладка не
  в фокусе, без изменения существующего API.
- Не показывать «задним числом» уже существующие на момент открытия вкладки
  непрочитанные уведомления как «новые» — иначе при каждой перезагрузке
  страницы пользователь получал бы лавину системных уведомлений о давно
  известных событиях.

**Non-Goals:**
- Push-уведомления при закрытом браузере (Service Worker + Web Push) — не
  входит, см. `proposal.md`.
- Настройка пользователем (вкл/выкл) показа системных уведомлений отдельно от
  браузерного разрешения — не запрашивалась.

## Decisions

### Диф по `Set<id>` в `useRef`, без персистентности между перезагрузками

`useDesktopNotifications` хранит `Set` увиденных id в `useRef` (не
`localStorage`) — при перезагрузке страницы весь текущий список считается
«базовым» и не порождает уведомлений. Отклонено: персистентность в
`localStorage` дала бы уведомления о событиях, случившихся, пока вкладка была
полностью закрыта — что противоречит собственному условию пользователя
(«лишь бы был открыт [браузер с вкладкой]», то есть контракт не
предполагает пропущенных при закрытой вкладке событий как обязанность этой
фичи).

### Разрешение запрашивается при монтировании, не по клику

`Notification.requestPermission()` вызывается в `useEffect` при первом
рендере `NotificationBell`, а не по явному действию пользователя (кнопка
«Включить уведомления» не вводится) — минимальный интерфейс, соответствует
формулировке запроса. Компромисс: некоторые браузеры (Chrome) могут
приглушать автоматический запрос разрешения эвристикой анти-спама после
повторных отказов пользователя — не обрабатывается отдельно, стандартное
поведение браузера.

## Тестовая стратегия

TDD-порядок не соблюдён (как и в родительском change `filial-corrections-overview`,
реализация велась итеративно в диалоге). Автотестов нет — см. `test-plan.md`.
Ручная проверка показа реального системного уведомления не выполнена (нет
доступа к GUI браузера из среды агента).

## Files / Owners

- **Frontend**: `apps/frontend/src/hooks/use-desktop-notifications.ts` (новый),
  `apps/frontend/src/components/notification-bell/notification-bell.tsx`
  (дополнен).

**Readiness Decision**: `blocked` — конфликт с `notifications-center` (см.
раздел выше) должен быть разрешён явным решением (обновить исходную спеку
или формализовать `MODIFIED` после архивации) до того, как этот change можно
считать `ready`; функционально код уже работает и покрыт статическими
проверками, но не автотестами.
