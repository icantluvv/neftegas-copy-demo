## API

_(нет задач)_

## Backend

_(нет задач)_

## Frontend

- [x] 3.1 [frontend] `SelectTrigger`: добавить `cursor-pointer` для визуальной
      индикации интерактивности
- [x] 3.2 [frontend] `SelectContent`: добавить внутренние отступы и gap между
      пунктами списка
- [x] 3.3 [frontend] `ErrorFallback`: заменить нативный `<button>` на `Button` из
      `#/components/ui/button` (`variant="outline"`), чтобы единственная кнопка
      вне UI-кита следовала общим стилям и состояниям
