## Риск

P3 — статическая правка справочных данных, без новой бизнес-логики. Автотесты
не пишутся (нет поведения для failing-теста); проверка — ручная, по трём
шагам ниже.

## TDD workflow

Не применим (см. `proposal.md` → «Влияние на качество» → TDD-порядок).

## Покрытие

Delta-спецификации не создавались (см. `proposal.md` → Capabilities) —
таблица покрытия MUST/SHALL сценариев не ведётся, вместо неё — таблица
ручных проверок ниже.

## Manual checks

| # | Проверка | Ожидаемый результат | Статус |
|---|---|---|---|
| 1 | Чистая БД + `bun run seed` содержит строку `cfos` со значением «СОВОФ», не «СОРиСОФ» | `SELECT code FROM cfos WHERE code IN ('СОРиСОФ','СОВОФ');` → ровно одна строка, `СОВОФ` | Planned |
| 2 | На уже засеянной среде (`infra-db-1`) выполнен `UPDATE cfos SET code='СОВОФ', name='СОВОФ' WHERE code='СОРиСОФ';` + `UPDATE users SET username='cfo.sovof@demo.local' WHERE username='cfo.sorisof@demo.local';` | `SELECT` подтвердил обе строки обновлены | Done |
| 3 | `GET /api/corrections/{humanId}` (авторизация `filial.donbassgaz@demo.local`) возвращает `code: "СОВОФ"` в `availableCfos` | Подтверждено `curl` — `COR-000010` содержит `"code":"СОВОФ"` среди `availableCfos`, «СОРиСОФ» не встречается | Done |
| 4 | Логин `cfo.sovof@demo.local` / `Password123` через `POST /api/auth/login` | `200`, сессия создаётся | Done |
| 5 | Визуальная проверка в браузере (клик по UI, не curl) | Не выполнена в этой сессии — API-проверка (п.3–4) покрывает те же данные, что рендерит UI (значение приходит из того же поля ответа) | Waiver — см. обоснование |
| 6 | История уже существующих (до переименования) корректировок с текстом действия этого ЦФО сохраняет исторический текст «СОРиСОФ …» неизменным (неизменяемый лог) | `CorrectionHistoryEntry.text` для старых записей не переписан | Confirmed by design (неизменяемый лог, правка не требуется) |

## Test data

Локальный dev-стек этой сессии (`infra-db-1`, контейнеры `infra-backend-1`/
`infra-frontend-1`, см. предыдущий запуск через `docker compose -f
infra/docker-compose.yml ps`) — уже засеян, содержит строку `cfos` со
значением «СОРиСОФ»; проверки 2–4 выполняются на нём после ручного `UPDATE`.
Проверка 1 (чистая БД) — либо новая база в CI/локально, либо после `docker
compose down -v` + пересоздания на этом же стеке (разрушительно для остальных
демо-данных сессии — по умолчанию не выполнять без явного запроса
пользователя).

## Verification gates

- Нет статических/юнит/e2e-гейтов — правка не .ts-логики уровня, требующего
  `tsc`/`eslint`/тестов (только строковый литерал в сиде и Markdown).
- Ручные проверки из таблицы выше — обязательны перед архивированием change
  (задача 4.4 `tasks.md`).

## Журнал уточнений

- 2026-08-24 — создание change по запросу пользователя («изменить ЦФО
  СОРиСОФ и заменить на СОВОФ», «все делай через openspec propose»).
  Зафиксировано: краткое техническое изменение, delta specs не создаются
  (обоснование — `proposal.md` → Capabilities). Открытый вопрос про
  переименование demo-username вынесен в `design.md` → Открыто, ждём ответа
  пользователя.
- 2026-08-24 — пользователь подтвердил оба открытых вопроса: (1) применить
  SQL UPDATE к уже поднятому dev-стеку сейчас — выполнено на `infra-db-1`;
  (2) переименовать demo-логин ЦФО для согласованности —
  `cfo.sovof@demo.local`, выполнено тем же SQL-шагом. `design.md` → Открыто
  и `tasks.md` (2.2, 4.2) обновлены.
