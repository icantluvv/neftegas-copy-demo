## Риск

P1 — изменение обязательного бизнес-правила комплектности пакета.

## TDD workflow

Backend: failing unit-тест в `corrections.service.spec.ts` (через `send()`,
`checkPackageComplete` приватный) → минимальная реализация → green →
refactor. Frontend: failing component-тест на рендер группы → реализация →
green.

## Покрытие сценариев

| Requirement | Scenario | Приоритет | Тип теста | Файл | Статус |
|---|---|---|---|---|---|
| Состав обязательного пакета документов | Создание корректировки формирует новый состав слотов | P1 | Unit + Manual | `corrections.service.spec.ts` | Done (юнит — регресс через `checkPackageComplete`; ручная — `COR-000011` содержит все 5 слотов, включая основной Excel) |
| Комплектность группы «выбор одного» | Ни один вариант не заполнен — пакет неполон | P0 | Unit + Manual | `corrections.service.spec.ts` | Done |
| Комплектность группы «выбор одного» | Заполнен один из двух — пакет полон | P0 | Unit + Manual | `corrections.service.spec.ts` | Done |
| Комплектность группы «выбор одного» | Заполнены оба — пакет полон | P1 | Unit | `corrections.service.spec.ts` | Done |

## Backend Unit

`apps/backend/src/corrections/corrections.service.spec.ts` — новый
describe-блок `checkPackageComplete (choice groups)`, фикстуры типа
корректировки с двумя альтернативными требованиями (`choiceGroupKey:
'mtr_package'`) плюс независимыми обязательными требованиями — регресс
против старого поведения.

## Frontend Component

`package-completeness.component.test.tsx` — рендер `detail.slots` с двумя
строками одинакового `choiceGroupKey`: заголовок группы виден один раз,
колонка «Обязателен» показывает контекстный текст (см. `design.md`).

## Test data

`apps/backend/src/database/seed.ts` — обновлённый состав `requirements`
(2.3.1) и переписанный `seedCorrection()` (2.3.2) обеспечивают демо-данные
для UI-разработки без ручного прохождения сценария.

## Manual checks

- [x] На `infra-db-1` после SQL-обновления (2.3.3): создана корректировка
      `COR-000011` через `POST /api/corrections`, `missingRequirements`
      содержит все 4 пункта, включая «Перечень комплекта МТР (ХС) (один из:
      Локальный сметный расчёт (ПД) / ХЗ-х ТКП)» — подтверждено `curl`.
- [x] Загружен файл в слот «Локальный сметный расчёт (ПД)» (не трогая «ХЗ-х
      ТКП») → `missingRequirements` для группы МТР пропал, остальные 3
      независимых пункта остались (документы туда не грузились) —
      подтверждено `curl` до/после.
- [x] `package-completeness.tsx` реализован (3.1.2): `curl` под
      `filial.donbassgaz@demo.local` подтвердил, что новая корректировка
      (`COR-000015`, удалена после проверки) получает ровно 5 слотов в
      нужном порядке — «Excel корректировка», «Согласованная служебная
      записка», «Пакет обосновывающих документов», «Локальный сметный
      расчёт (ПД)» и «ХЗ-х ТКП» (оба последних с `choiceGroupKey:
      'mtr_package'`, `groupLabel: 'Перечень комплекта МТР (ХС)'») —
      данные, которые рендерит компонент, корректны.
- [ ] Визуальная проверка блока «Комплектность пакета» в браузере (клик по
      UI, действительно ли надпись группы видна и колонка «Обязателен»
      показывает «Да (один из группы)») — **waiver**: в dev-контейнере
      frontend нет установленного Chromium для Playwright/Vitest Browser
      Mode (то же ограничение, что и в остальных changes этой сессии, см.
      `notification-auto-read-on-open/test-plan.md`).

## Verification gates

- API: `npm run lint` из `api/`.
- Backend: `bun run lint`, `bun run test` (новые кейсы `checkPackageComplete`).
- Frontend: `bun run typecheck`, `bun run lint`, `bun run test`.

## Журнал уточнений

- 2026-08-24 — создание change по запросу пользователя: новый состав пакета
  (4 пункта), пункт 4 — группа «выбор одного из двух» (уточнено в диалоге:
  изначально упомянут гипотетический «4.3», пользователь подтвердил, что его
  нет — ровно два варианта, 4.1 и 4.2). Текст «ХЗ-х ТКП» подтверждён
  пользователем как точный (не опечатка). Дизайн группового блока не
  передан — раздел `design.md` → «Дизайн (UI)» содержит решение по умолчанию,
  ждёт подтверждения/замены.
- 2026-08-24 — backend реализован и проверен сквозным `curl`-сценарием на
  `infra-db-1`: создание корректировки → 4 недостающих пункта в
  `missingRequirements` (группа МТР — одной строкой) → загрузка файла в один
  вариант группы → группа пропадает из недостающих, второй вариант остаётся
  пустым без блокировки. По пути обнаружены и исправлены два
  пред­существующих бага, не связанных с этим change по содержанию, но
  блокировавших его проверку — см. `proposal.md` → «Побочная находка» и
  `tasks.md`, 2.3.4–2.3.5. Backend-тесты 40/40 green.
- 2026-08-25 — по запросу пользователя синхронизированы «замороженные»
  `document_slots.label` уже существующих корректировок (`COR-000001`,
  `COR-000002`) с текущими именами `package_requirements` (SQL `UPDATE` на
  `infra-db-1`), и добавлен недостающий 5-й слот («ХЗ-х ТКП», `requirementId
  = 4`) — эти корректировки были созданы до появления 4-го требования в
  справочнике и физически не могли получить этот слот при создании. Только
  для dev/demo окружения (`infra-db-1`), не общий backend-механизм.
- 2026-08-25 — API-схема (`choiceGroupKey`/`groupLabel` в `DocumentSlot`,
  1.1) и frontend (`package-completeness.tsx`, 3.1.1–3.1.2) реализованы:
  Kubb-клиент перегенерирован, компонент рендерит надпись `groupLabel —
  выберите один вариант` над первой строкой группы и «Да (один из группы)»
  в колонке «Обязателен». Проверено `curl`-запросом создания новой
  корректировки (см. Manual checks). Component-тест и визуальная проверка в
  браузере — waiver (нет Chromium в dev-контейнере фронтенда).
