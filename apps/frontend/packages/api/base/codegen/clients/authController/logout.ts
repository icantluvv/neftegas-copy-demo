/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "../../../client";
import type { Client, RequestConfig, ResponseErrorConfig } from "../../../client";
import type { LogoutMutationResponse } from "../../types/authController/Logout";
import { logoutMutationResponseSchema } from "../../zod/authController/logoutSchema";

function getLogoutUrl() {
  const res = { method: 'POST', url: `/auth/logout` as const }
  return res
}

/**
 * @summary Выход — завершение текущей сессии, очистка cookie
 * {@link /auth/logout}
 */
export async function logout(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const { client: request = fetch, ...requestConfig } = config



  const res = await request<LogoutMutationResponse, ResponseErrorConfig<Error>, unknown>({ method : "POST", url : getLogoutUrl().url.toString(), ... requestConfig })
  return logoutMutationResponseSchema.parse(res.data)
}